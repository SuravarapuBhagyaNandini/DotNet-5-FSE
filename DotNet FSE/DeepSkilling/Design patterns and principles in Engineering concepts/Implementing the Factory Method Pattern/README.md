# Implementing the Factory Method Pattern

Exercise: Implement the Factory Method design pattern.

Contents:
- Example classes demonstrating the Factory Method.
- `Main.java` to run a demo.

Instructions:
1. Add source files for products and creator classes.
2. Compile with `javac` and run with `java` (or your IDE).
