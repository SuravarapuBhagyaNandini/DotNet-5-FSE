# Implementing the Singleton Pattern

Exercise: Implement the Singleton design pattern.

Contents:
- `Logger.java` (singleton implementation example).
- `Main.java` to run and validate the singleton.

Instructions:
1. Add `Logger.java` implementing a thread-safe singleton.
2. Compile and run using `javac` and `java`.
